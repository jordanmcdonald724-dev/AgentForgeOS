# Scripts

This directory is reserved for operational and automation scripts per the Build Bible V2 required root structure.

## Release

- [release_smoke.ps1](file:///d:/AgentForgeOS/scripts/release_smoke.ps1): validates a built desktop folder (layout + backend health + workspace dirs).
- [build_release.ps1](file:///d:/AgentForgeOS/scripts/build_release.ps1): runs tests, builds backend.exe, builds Electron installer.
